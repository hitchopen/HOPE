# Quickstart

The canonical quickstart lives at the repository root:
**[QUICKSTART_A3_ISAAC.md](../QUICKSTART_A3_ISAAC.md)**

It walks the full loop — prepare the A3 asset, smoke-test the table-tennis scene,
train the deploy-grade rally policy (`task=HitterPingPong`),
evaluate it in Isaac, export the ONNX policy, verify it with the
MuJoCo sim-to-sim evaluator, and rehearse the C++ deploy runner closed-loop with
the planner. This file is only a pointer so older links keep working; do not add
content here.
