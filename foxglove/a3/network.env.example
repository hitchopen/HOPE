# Copy to /etc/hope-foxglove/network.env on the HDU.
# Use the Laptop Wi-Fi address reachable from the HDU. Multiple peers are
# separated with semicolons. Leave empty only when subnet multicast discovery
# has been verified at the venue.
ROS_AUTOMATIC_DISCOVERY_RANGE=SUBNET
ROS_STATIC_PEERS=
